<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/template.css">
  <title>Majster</title>
</head>
<body>

  <div class="vh-100 d-flex justify-content-center align-items-center">
    <div class="container">
      <div class="row d-flex justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
          <div class="card bg-white shadow-lg">
            <div class="card-body p-5">

              <form class="mb-2" action="/Login" method="POST">
                <div class="mb-4 d-flex justify-content-center align-items-center">
                  <img src="/images/logo.png" alt="logo" width="100px" height="100px">
                </div>
                <p class="mb-4 d-flex justify-content-center align-items-center">Witaj w systemie Majster, proszę się zalogować!</p>
                <div class="mb-3">
                  <label for="login" class="form-label">Login</label>
                  <input type="login" class="form-control" id="login" name="login" placeholder="Login">
                </div>
                <div class="mb-3">
                  <label for="password" class="form-label ">Hasło</label>
                  <input type="password" class="form-control" id="password" name="password" id="password" placeholder="******">
                </div>
                <div class="d-flex justify-content-center align-items-center">
                  <input type="submit" class="btn btn-block btn btn-warning" value="Zaloguj się">
                </div>
              </form>

              <div class="d-flex justify-content-center align-items-center">
                <% if(messages.success_msg) { %> 
                  <%= messages.success_msg %>
                <% }  %> 

                <% if (messages.error) { %>
                  <%= messages.error %>
                <% } %> 
              </div>
              
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
