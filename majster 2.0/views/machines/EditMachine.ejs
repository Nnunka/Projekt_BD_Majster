<%- include('../components/Head')%>

<body>

  <header>
    <%- include('../components/Navbar')%>
  </header>

  <div class="ml-3 mt-3 mb-3"><ul><b>
    <% if(typeof errors != 'undefined') { %> 
      <% errors.forEach(error=>{%>
        <li><%= error.message %></li> 
    <% })  %> <% } %>
  </b></ul></div>

  <div class="vh-100 d-flex justify-content-left align-items-top mt-4 ml-4">
    <form action="/machines/EditMachine/<%= machineId %>" method="POST">
      <div class="form-group">
        <label for="">Nazwa</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= machineData.machine_name %>" maxlength="20" required>
      </div>
      <div class="form-group">
        <label for="">Rodzaj</label>
        <input type="text" class="form-control" id="type" name="type" value="<%= machineData.machine_type %>" maxlength="20" required>
      </div>
      <button type="submit" class="btn btn-warning">Edytuj maszyne</button>
    </form>
  </div>

</body>